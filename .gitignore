# Build output
/bin/
/web/dist/
/web/node_modules/
/web/.vite/

# Local runtime state
/data/
/config.toml
*.db
*.db-wal
*.db-shm

# Secrets — never committed; see config.example.toml
secret.key
# direnv: carries RENTAL_BOT_SECRET_KEY into the shell
.envrc

.playwright-mcp/
